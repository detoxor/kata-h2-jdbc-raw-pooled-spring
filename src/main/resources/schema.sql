/**
 * @author tomas.cejka
 * @since 16.3.2017
 */
CREATE TABLE person(
   id bigint NOT NULL AUTO_INCREMENT,
   first_name varchar(20) NOT NULL,
   last_name  varchar(20) NOT NULL,
   primary key (ID)
);
